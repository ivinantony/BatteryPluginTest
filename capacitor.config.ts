import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'BatteryPlugin',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
